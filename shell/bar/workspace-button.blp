using Gtk 4.0;
using Adw 1;
using AstalWorkspace 0.1;

template $WorkspaceButton: Adw.Bin {
    styles [
        "workspace",
    ]

    Label {
        label: bind template.workspace as <AstalWorkspace.Workspace>.name;
    }

    GestureClick {
        released => $handle_click();
    }
}
